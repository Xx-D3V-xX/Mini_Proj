# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Port on which the AI models service will run
PORT=8001

# =============================================================================
# EMBEDDING MODEL CONFIGURATION
# =============================================================================
# Sentence transformer model for POI embeddings and similarity search
# This model will be downloaded automatically on first run (no API key needed)
# Alternative models: sentence-transformers/paraphrase-MiniLM-L6-v2
EMBED_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Directory to cache the downloaded embedding model
EMBED_CACHE_DIR=./app/data/cache

# =============================================================================
# RECOMMENDATION ENGINE
# =============================================================================
# Weights for recommendation algorithm (category, tags, price, rating, distance)
# These control how much each factor influences POI recommendations
# Format: category_weight, tags_weight, price_weight, rating_weight, distance_weight
RECO_WEIGHTS=0.45,0.2,0.2,0.05,0.1

# =============================================================================
# ROUTING SERVICE (OPTIONAL)
# =============================================================================
# OSRM (Open Source Routing Machine) for accurate route calculations
# Leave as localhost if running OSRM locally, or use a public OSRM instance
# If not available, the system will use fallback distance calculations
OSRM_URL=http://localhost:5000

# =============================================================================
# GOOGLE GEMINI AI (OPTIONAL BUT RECOMMENDED)
# =============================================================================
# Google Gemini API key for chatbot and natural language processing
# Get your FREE API key at: https://aistudio.google.com/app/apikey
# The chatbot will work with basic functionality even without this key
# With the key, you get enhanced conversational AI capabilities
GEMINI_API_KEY=

# Gemini model to use (gemini-2.5-flash is recommended for speed and cost)
# Options: gemini-2.5-flash, gemini-1.5-pro, gemini-1.5-flash
GEMINI_MODEL=gemini-2.5-flash

# Rate limiting for Gemini API (requests per minute)
# Free tier allows 15 RPM, adjust based on your quota
GEMINI_REQUESTS_PER_MINUTE=15

# =============================================================================
# DATA SOURCES
# =============================================================================
# Path to POI CSV file for loading attraction data
POI_CSV_PATH=../backend/seed/pois.csv
