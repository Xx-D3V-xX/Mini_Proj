Convert the last chat turn (user + assistant) into compact memories.
Each memory must match memory_item.schema.json and include ttl_days suggestion.
Only store facts that will help future recommendations (preferences, constraints, recurring facts).
