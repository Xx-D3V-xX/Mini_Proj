# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL connection string
# Format: postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE_NAME
# For local development, use the default postgres user or create a new one
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mumbai_trails

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Port on which the backend server will run
PORT=4000

# =============================================================================
# JWT AUTHENTICATION
# =============================================================================
# Secret keys for JWT tokens (CHANGE THESE IN PRODUCTION!)
# Use strong, random strings. Generate with: openssl rand -base64 32
JWT_ACCESS_SECRET=your-secret-access-key-change-in-production-min-32-chars
JWT_REFRESH_SECRET=your-secret-refresh-key-change-in-production-min-32-chars

# Token expiration times
ACCESS_EXPIRES_MIN=15
REFRESH_EXPIRES_DAYS=7

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Frontend URL for CORS (comma-separated for multiple origins)
CORS_ORIGIN=http://localhost:5173

# =============================================================================
# AI MODELS SERVICE
# =============================================================================
# Base URL of the AI models service (Python FastAPI)
AI_BASE_URL=http://localhost:8001

# =============================================================================
# AUTHENTICATION
# =============================================================================
# Use HTTP-only cookies for auth tokens (recommended for production)
USE_AUTH_COOKIES=true

# =============================================================================
# WEATHER API (OPTIONAL)
# =============================================================================
# Weather mode: 'mock' for testing, 'api' for real weather data
WEATHER_MODE=mock

# OpenWeatherMap API key (only needed if WEATHER_MODE=api)
# Get your free API key at: https://openweathermap.org/api
# Leave empty to use mock weather data
WEATHER_API_KEY=

# =============================================================================
# GOOGLE MAPS API (OPTIONAL)
# =============================================================================
# Google Maps API key for route optimization and ETA calculations
# Get API key at: https://console.cloud.google.com/google/maps-apis
# Required APIs: Directions API, Distance Matrix API, Places API
# Leave empty to use fallback route calculations
GOOGLE_MAPS_API_KEY=
