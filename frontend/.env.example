# =============================================================================
# API CONFIGURATION
# =============================================================================
# Backend API base URL (without trailing slash)
# For local development, use localhost
# For production, use your deployed backend URL
VITE_API_BASE=http://localhost:4000/api

# =============================================================================
# MAP CONFIGURATION
# =============================================================================
# OpenStreetMap tile server URL for map rendering
# This is free and doesn't require an API key
# Alternative: Use Mapbox tiles if you have a Mapbox API key
# Mapbox format: https://api.mapbox.com/styles/v1/mapbox/streets-v11/tiles/{z}/{x}/{y}?access_token=YOUR_MAPBOX_TOKEN
VITE_MAP_TILE_URL=https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png

# =============================================================================
# GOOGLE MAPS (OPTIONAL)
# =============================================================================
# Google Maps API key for enhanced map features (optional)
# Get API key at: https://console.cloud.google.com/google/maps-apis
# Leave empty to use OpenStreetMap only
VITE_GOOGLE_MAPS_API_KEY=
